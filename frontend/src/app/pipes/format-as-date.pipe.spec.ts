import { FormatAsDatePipe } from './format-as-date.pipe';

describe('FormatAsDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatAsDatePipe();
    expect(pipe).toBeTruthy();
  });
});
