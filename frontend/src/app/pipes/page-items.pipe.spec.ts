import { PageItemsPipe } from './page-items.pipe';

describe('PageItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new PageItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
